# Python 3.10+ (dev) / 3.12 (docker image)

# API server
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1

# Configuration and validation
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
email-validator>=2,<3

# PostgreSQL
sqlalchemy[asyncio]>=2.0,<2.2
asyncpg>=0.30,<1

# Redis (ephemeral challenges, rate limits, usage metering)
redis>=6,<9

# HTTP (Resend, LLM upstream proxy)
httpx>=0.28,<1

# JWT
PyJWT>=2.10,<3

# Serialization
orjson>=3.10,<4
